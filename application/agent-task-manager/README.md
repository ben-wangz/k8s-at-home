# agent-task-manager

`agent-task-manager` is the Go-based rebuild of the current `pith` task system.

## Layout

- `chart/`: Helm chart
- `backend/`: Go API
- `cli/`: Go CLI client
- `frontend/`: web UI
- `design.md`: implementation design

## Status

This directory currently contains the initial project skeleton only.
