import { inlineActionButtonStyle, inlineGhostActionButtonStyle, surfaceStyle, tableCellStyle, tableHeadStyle, tableStyle, toolbarStyle, toolbarTitleStyle } from './styles'
import type { Session } from './types'
import { WorkspaceHeader } from './view-shared'

export function SessionsView({ sessions, onPreview, onDownload }: { sessions: Session[]; onPreview: (snapshotId: string) => void; onDownload: (snapshotId: string) => void }) {
	return <div style={{ display: 'grid', gap: 18 }}><WorkspaceHeader eyebrow="Sessions" title="Registry for imported agent session snapshots" description="A document-registry mental model: upload, register, inspect metadata, and download artifacts without pretending these are task cards." /><div style={surfaceStyle}><div style={toolbarStyle}><div style={toolbarTitleStyle}>Snapshot Registry</div></div><div style={{ overflowX: 'auto' }}><table style={tableStyle}><thead><tr>{['Title', 'Snapshot ID', 'Description', 'Artifact Name', 'Artifact Path', 'Updated', 'Actions'].map((header) => <th key={header} style={tableHeadStyle}>{header}</th>)}</tr></thead><tbody>{sessions.map((session) => <tr key={session.snapshotId}><td style={tableCellStyle}><div style={{ fontWeight: 700 }}>{session.title}</div></td><td style={{ ...tableCellStyle, fontFamily: 'ui-monospace, SFMono-Regular, monospace', fontSize: 12 }}>{session.snapshotId}</td><td style={tableCellStyle}>{session.description}</td><td style={tableCellStyle}>{session.artifactName}</td><td style={{ ...tableCellStyle, maxWidth: 280 }}><div style={{ whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis', fontFamily: 'ui-monospace, SFMono-Regular, monospace', fontSize: 12 }}>{session.artifactPath}</div></td><td style={tableCellStyle}>{session.updatedAt}</td><td style={tableCellStyle}><div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}><button style={inlineActionButtonStyle} onClick={() => onPreview(session.snapshotId)}>Preview</button><button style={inlineGhostActionButtonStyle} onClick={() => onDownload(session.snapshotId)}>Download</button></div></td></tr>)}</tbody></table></div></div></div>
}
