{{- include "sshpiper.validateValues" . }}
apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
kind: Deployment
metadata:
  name: {{ include "common.names.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.replicas }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  {{- if .Values.updateStrategy }}
  strategy: {{- toYaml .Values.updateStrategy | nindent 4 }}
  {{- end }}
  selector:
    matchLabels: {{- include "common.labels.matchLabels" ( dict "customLabels" .Values.podLabels "context" $ ) | nindent 6 }}
  template:
    metadata:
      labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.podLabels "context" $ ) | nindent 8 }}
      annotations:
        {{- if .Values.podAnnotations }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.podAnnotations "context" $ ) | nindent 8 }}
        {{- end }}
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets: {{- include "common.tplvalues.render" ( dict "value" .Values.imagePullSecrets "context" $ ) | nindent 8 }}
      {{- end }}
      {{- if .Values.affinity }}
      affinity: {{- include "common.tplvalues.render" ( dict "value" .Values.affinity "context" $ ) | nindent 8 }}
      {{- else }}
      affinity:
        podAffinity: {{- include "common.affinities.pods" ( dict "type" .Values.podAffinityPreset "customLabels" .Values.podLabels "context" $ ) | nindent 10 }}
        podAntiAffinity: {{- include "common.affinities.pods" ( dict "type" .Values.podAntiAffinityPreset "customLabels" .Values.podLabels "context" $ ) | nindent 10 }}
        nodeAffinity: {{- include "common.affinities.nodes" ( dict "type" .Values.nodeAffinityPreset.type "key" .Values.nodeAffinityPreset.key "values" .Values.nodeAffinityPreset.values ) | nindent 10 }}
      {{- end }}
      {{- if .Values.nodeSelector }}
      nodeSelector: {{- include "common.tplvalues.render" ( dict "value" .Values.nodeSelector "context" $ ) | nindent 8 }}
      {{- end }}
      {{- if .Values.tolerations }}
      tolerations: {{- include "common.tplvalues.render" ( dict "value" .Values.tolerations "context" $ ) | nindent 8 }}
      {{- end }}
      {{- if .Values.podSecurityContext.enabled }}
      securityContext: {{- include "common.compatibility.renderSecurityContext" ( dict "secContext" .Values.podSecurityContext "context" $ ) | nindent 8 }}
      {{- end }}
      containers:
        - name: sshpiper
          image: {{ include "common.images.image" (dict "imageRoot" .Values.image "global" .Values.global) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if .Values.containerSecurityContext.enabled }}
          securityContext: {{- include "common.compatibility.renderSecurityContext" ( dict "secContext" .Values.containerSecurityContext "context" $ ) | nindent 12 }}
          {{- end }}
          command:
            - /sshpiperd/sshpiperd
          args:
            - {{ .Values.config.pluginPath }}
          ports:
            - name: ssh
              containerPort: {{ .Values.listen.port }}
          env:
            - name: SSHPIPERD_ADDRESS
              value: {{ .Values.listen.address | quote }}
            - name: SSHPIPERD_PORT
              value: {{ .Values.listen.port | quote }}
            - name: SSHPIPERD_SERVER_KEY
              value: /keys/{{ .Values.serverKey.secretKey }}
            - name: SSHPIPERD_SERVER_KEY_GENERATE_MODE
              value: disable
            - name: SSHPIPER_ALLOWED_TARGETS
              value: {{ include "sshpiper.allowedTargetsCSV" . | quote }}
            - name: SSHPIPER_AUTH_MODE
              value: {{ .Values.config.authMode | quote }}
            - name: SSHPIPER_HOSTKEY_POLICY
              value: {{ .Values.config.hostKeyPolicy | quote }}
            - name: SSHPIPER_KNOWN_HOSTS_PATH
              value: /var/sshpiper/known_hosts/known_hosts
            - name: SSHPIPER_AUTHORIZED_KEYS_PATH
              value: {{ .Values.authFiles.authorizedKeysPath | quote }}
            - name: SSHPIPER_UPSTREAM_PRIVATE_KEY_PATH
              value: {{ .Values.authFiles.upstreamPrivateKeyPath | quote }}
            {{- if .Values.extraEnvVars }}
            {{- include "common.tplvalues.render" (dict "value" .Values.extraEnvVars "context" $) | nindent 12 }}
            {{- end }}
          {{- if .Values.extraEnvVarsSecret }}
          envFrom:
            - secretRef:
                name: {{ .Values.extraEnvVarsSecret }}
          {{- end }}
          volumeMounts:
            - name: server-key
              mountPath: /keys
              readOnly: true
            - name: known-hosts
              mountPath: /var/sshpiper/known_hosts
            {{- if .Values.authFiles.existingSecret }}
            - name: auth-files
              mountPath: {{ .Values.authFiles.mountPath | quote }}
              readOnly: true
            {{- end }}
            {{- if .Values.extraVolumeMounts }}
            {{- include "common.tplvalues.render" (dict "value" .Values.extraVolumeMounts "context" $) | nindent 12 }}
            {{- end }}
          {{- if .Values.resources }}
          resources: {{- toYaml .Values.resources | nindent 12 }}
          {{- else if ne .Values.resourcesPreset "none" }}
          resources: {{- include "common.resources.preset" (dict "type" .Values.resourcesPreset) | nindent 12 }}
          {{- end }}
          {{- if .Values.customLivenessProbe }}
          livenessProbe: {{- include "common.tplvalues.render" (dict "value" .Values.customLivenessProbe "context" $) | nindent 12 }}
          {{- else }}
          livenessProbe:
            tcpSocket:
              port: ssh
            initialDelaySeconds: 30
            periodSeconds: 10
          {{- end }}
          {{- if .Values.customReadinessProbe }}
          readinessProbe: {{- include "common.tplvalues.render" (dict "value" .Values.customReadinessProbe "context" $) | nindent 12 }}
          {{- else }}
          readinessProbe:
            tcpSocket:
              port: ssh
            initialDelaySeconds: 10
            periodSeconds: 5
          {{- end }}
          {{- if .Values.customStartupProbe }}
          startupProbe: {{- include "common.tplvalues.render" (dict "value" .Values.customStartupProbe "context" $) | nindent 12 }}
          {{- else }}
          startupProbe:
            tcpSocket:
              port: ssh
            initialDelaySeconds: 20
            periodSeconds: 10
          {{- end }}
      volumes:
        - name: server-key
          secret:
            secretName: {{ include "sshpiper.serverKeySecretName" . }}
        - name: known-hosts
          {{- if eq .Values.storage.knownHosts.type "pvc" }}
          persistentVolumeClaim:
            claimName: {{ required "storage.knownHosts.existingClaim is required when storage.knownHosts.type is pvc" .Values.storage.knownHosts.existingClaim }}
          {{- else }}
          emptyDir: {}
          {{- end }}
        {{- if .Values.authFiles.existingSecret }}
        - name: auth-files
          secret:
            secretName: {{ .Values.authFiles.existingSecret }}
        {{- end }}
        {{- if .Values.extraVolumes }}
        {{- include "common.tplvalues.render" (dict "value" .Values.extraVolumes "context" $) | nindent 8 }}
        {{- end }}
