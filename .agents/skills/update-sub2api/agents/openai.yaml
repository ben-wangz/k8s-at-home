interface:
  display_name: "Update sub2api image"
  short_description: "Check and update the official sub2api image"
  default_prompt: "Use $update-sub2api to check and update only the official sub2api image tag."
